export enum StorefrontConfigurationType {
  FEATURED_PRODUCTS = 'featured_products',
  CATEGORY_PRODUCTS = 'category_products',
  NEW_PRODUCTS = 'new_products',
  NAV_ITEM = 'nav_item',
}

export enum StorefrontConfigurationSection {
  PRODUCT_SECTIONS = 'product-sections',
  CATEGORIES_PANEL = 'categories-panel',
}
